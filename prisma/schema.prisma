generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Place {
  place_id  String @id
  formatted String
  postcode  Int
  street    String
  lat       Float
  lon       Float
}
