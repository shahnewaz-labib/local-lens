generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Place {
  id        Int    @id @default(autoincrement())
  place_id  String
  formatted String
  postcode  Int
  street    String
  lat       Float
  lon       Float
}
